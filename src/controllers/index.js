const authCtrl = require('./auth.controller')
const sellerCtrl = require('./seller.controller')
const buyerCtrl = require('./buyer.controller')

module.exports = {
    authCtrl,
    sellerCtrl,
    buyerCtrl
}